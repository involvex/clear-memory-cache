# Plan: Create AGENTS.md for clear-memory-cache

## Goal

Create a compact, high-signal AGENTS.md that helps future agents avoid mistakes and ramp up quickly. Every line must answer "Would an agent likely miss this without help?"

## Investigation Summary

### Repository Overview

- Windows CLI tool for clearing memory caches/standby memory via Rammap (Sysinternals)
- Built with TypeScript/ES Modules, React+Ink for terminal UI, Bun as runtime
- CLI entry: `src/cli.tsx` → routes to command components in `src/commands/`

### Key Sources Analyzed

- `readme.md` - User-facing docs, commands, examples
- `CLAUDE.md` - Existing dev guidance (commands, architecture)
- `package.json` - Dependencies, scripts, build config
- `tsconfig.json` - `@sindresorhus/tsconfig` base, ES modules, Node16
- `eslint.config.ts` - ESM config, TypeScript-ESLint
- `.editorconfig` - Tabs for code, spaces for YAML
- `src/cli.tsx` - meow-based CLI, command routing
- `src/commands/*.tsx` - Ink React components for each command
- `src/utils/execute.ts` - spawnSync wrapper for commands

### Non-Obvious Findings (High-Signal)

**Build/Type System:**

- ES Modules (`"type": "module"`) - all imports use `.js` extensions even for TS source
- `@sindresorhus/tsconfig` base config - non-standard TypeScript setup
- `prebuild` runs format → lint:fix → typecheck automatically
- Output to `dist/` directory, compiled to `dist/src/cli.js`

**Development:**

- Bun runtime (not Node/npm) - `bun run dev` executes source directly via Bun
- `npm run dev` = `bun run src/cli.tsx` (esbuild-style dev execution)
- Imports reference compiled `.js` files (e.g., `import About from './commands/about.js'`)

**Testing:**

- Uses `ava` with `ts-node` loader (`--loader=ts-node/esm`)
- Test file imports use `.js` extensions (e.g., `./src/app.js` not `.tsx`)
- `test.tsx` uses ava + ink-testing-library
- `test` script runs: prettier check → xo → ava (sequential, all must pass)

**Code Style:**

- `.editorconfig`: tabs for code, 2-space indent for YAML files
- Prettier config: 80 width, singleAttributePerLine, specific import/plugins
- ESLint: TypeScript-ESLint with XO React config
- `xo` linter extends `xo-react`

**Architecture:**

- Commands are Ink React components (not plain functions)
- Each command imports from `ink` and `react`, uses hooks
- `clear-pm-cache` and `clear-all` use `spawnSync` from `node:child_process`
- Shared types in `src/commands/types/index.ts`
- `windowsHide: true` for Windows subprocess execution

**Prerequisites (from README):**

- Rammap (Sysinternals) must be installed and in PATH
- Windows only (PowerShell / Windows Terminal)

### What's Missing from Existing CLAUDE.md

- No mention of ES Modules `.js` import convention for TS source
- No Bun-specific dev workflow details
- No testing setup details (ava + ts-node loader)
- No editorconfig/prettyttier config details
- No prebuild hook behavior
- No code style specifics (tabs vs spaces)

## AGENTS.md Content Plan

Will organize into sections:

1. **Quick Start** - immediate commands to run the project
2. **Architecture** - non-obvious structural decisions
3. **Development** - dev workflow quirks (Bun, ESM, .js imports)
4. **Building** - build process and prebuild hooks
5. **Testing** - test setup and quirks
6. **Code Style** - formatting/linting and config files
7. **Prerequisites** - external tools needed
8. **References** - link to CLAUDE.md and README for more

All content will be verified against actual config files and package.json scripts.
